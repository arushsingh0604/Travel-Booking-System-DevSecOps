// File: backend/app.test.js

// This is a "smoke test" to prove Jest is working.
test('basic addition', () => {
  expect(1 + 1).toBe(2);
});

test('backend placeholder test', () => {
  // This is a placeholder for a real test
  const isBackendRunning = true;
  expect(isBackendRunning).toBe(true);
});
